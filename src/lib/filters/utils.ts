/**
 * Utility functions for filter system
 */

/**
 * Initialize the filter system
 * Registers all available format loaders
 */
export function initializeFilterSystem(): void {
  // Import and register loaders
  // This will be called when the filter system is first used
  // Loaders are registered lazily to avoid circular dependencies
}
